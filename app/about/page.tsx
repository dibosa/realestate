export default function AboutPage() {
  return (
    <section style={{padding:'24px 0'}}>
      <h1>About</h1>
      <p>This is a lightweight real-estate starter you can deploy on Netlify. It uses static JSON for listings now; you can wire it to a database later.</p>
    </section>
  );
}
